# Analysis of Algorithms - Lab 04

- `knapsack_exhaustive.py` — 0/1 Knapsack solved via exhaustive search, with a timing benchmark table (n = 3 .. until a run exceeds 30s).
- `graph_dfs.py` — Depth First Search over a graph stored as an adjacency list.
- `graph_bfs.py` — Breadth First Search over a graph stored as an adjacency list.

Run any file with: `python3 <filename>.py`
